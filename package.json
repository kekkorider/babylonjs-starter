{
  "name": "babylonjs-starter",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:kekkorider/babylonjs-starter.git",
  "author": "Francesco Michelini <hello@francescomichelini.com>",
  "license": "MIT",
  "scripts": {
    "dev": "rm -rf dist && parcel src/index.html --no-source-maps",
    "build": "rm -rf dist && parcel build src/index.html --no-source-maps"
  },
  "dependencies": {
    "@babylonjs/core": "^4.1.0",
    "@babylonjs/inspector": "^4.1.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.3"
  }
}
